{
  "name": "x-framework",
  "version": "1.0.0",
  "description": "XFramework is a framework implementation of XFrames library.",
  "private": true,
  "scripts": {
    "dump-autoload": "composer dump-autoload",
    "webpack": "webpack --config webpack.config.js",
    "watch": "webpack --config webpack.config.js --watch",
    "build": "composer dump-autoload && webpack --config webpack.config.js",
    "serve": "concurrently \"cd public_html && php -S localhost:8008\" \"opener http://localhost:8008\""
  },
  "keywords": [
    "php"
  ],
  "author": "Sampan Verma",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0",
    "css-loader": "^3.5.3",
    "opener": "^1.5.1",
    "sass": "^1.26.5",
    "sass-loader": "^8.0.2",
    "style-loader": "^1.2.1",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}
